# Design

빌드된 결과물(`dist/index.html`)에서 기록한 것. 의도가 아니라 실제로 화면에 있는 것을 적는다.

## 세계 (World)

서울 오피스 로비의 **층별 안내판**. Bloom은 자기 공간이 없어 25번 모두 남의 층을 빌렸고, 그 사실이 페이지의 형식을 결정했다. 모든 구성요소는 판(plate) 아니면 행(row)이다. 카드 그리드, 그라디언트 레일, 히어로 메트릭 템플릿은 쓰지 않는다.

## 색

전략은 **절제(Restrained)** — 중성 잉크 + 단일 강조색(황동). 녹색은 상태 표시(예정)에만 쓰고 넓은 면적을 차지하지 않는다. 장면은 저녁 7시에 링크를 받아 여는 화면이므로 다크가 기본이고, 라이트는 에나멜 사이니지 판으로 뒤집는다.

| 토큰 | 다크 | 라이트 | 역할 |
|---|---|---|---|
| `--plate` | `#14161a` | `#eae6dc` | 판 바탕 |
| `--plate-2` | `#1b1e24` | `#f3f0e8` | 돋운 판 (회고·인용판) |
| `--rule` / `--rule-2` | `#2b3038` / `#3a4049` | `#cdc6b6` / `#b3aa96` | 괘선 |
| `--ink` | `#ece7dc` | `#16181c` | 각인 |
| `--ink-2` | `#a4a196` | `#5e5a50` | 2차 텍스트 |
| `--ink-3` | `#85837a` | `#6b6559` | 라벨·주석 |
| `--brass` | `#c8912f` | `#7f5710` | 층수·오늘·강조 |
| `--signal` | `#3f9d6d` | `#216e46` | 예정·처음 있었던 일 |

`--ink-2`, `--ink-3`, `--brass`, `--signal` 모두 각자의 바탕 위에서 4.5:1 이상. 반투명 배경(레일, 오늘 행)은 합성 후 값으로 검증한다 (`tools/check.mjs`).

## 활자

- **Overpass** (600/700/800) — 라틴·숫자·라벨. Highway Gothic 계열의 사이니지 서체. 날짜·수치는 `tabular-nums`.
- **Gothic A1** (500/800) — 한글 디스플레이. 제목과 인용문.
- 본문 한글은 시스템 스택 (Apple SD Gothic Neo / Pretendard / Noto Sans KR). 읽기용 표면이므로 워크호스 서체가 맞다.

두 웹폰트는 페이지에 실제로 쓰인 글자만 남겨 base64로 인라인한다 (총 142KB woff2). 자간 하한 -0.045em(디스플레이) / -0.03em(제목), 본문 측정폭 66ch, 한글은 `word-break: keep-all`.

## 구성

- **머리판** — BLOOM / 기간 / 리드 / 현황 스트립. 큰 숫자를 히어로로 세우지 않고 괘선으로 나눈 스트립에 눕힌다.
- **안내판** — 날짜 / 장소·층 / 행사명 / 규모 3열 행. 오늘 회차는 황동. 행을 누르면 판이 열린다.
- **펼친 판** — 넓은 화면에서 본문(66ch) | 세로 괘선 | 규격판(날짜·장소·후원·규모·포맷·언어·연사·링크). 좁은 화면에서는 본문 뒤에 규격판.
- 이하 섹션은 모두 제목(15rem) | 내용 2단 격자. 회고와 예정은 auto-fit 2단.

## 움직임

연출된 순간은 하나 — **판이 열리는 것**. `grid-template-rows: 0fr → 1fr`, 0.42s, `cubic-bezier(.16,1,.3,1)`. 열린 내용은 10px 아래에서 한 번 올라온다. 진입 애니메이션은 없다(행은 처음부터 보인다). `prefers-reduced-motion`에서 전부 정지.

## 브라우저 표면

선택 영역, 캐럿, 스크롤바, 포커스 링, 밑줄 오프셋을 모두 팔레트에서 지정한다. 아이콘은 그린 SVG만 쓴다 — 유니코드 글리프·이모지 없음.

## 상호작용

검색 / 시기·형식·표시 필터 / 행 펼치기 / 전부 펼치기 / 주제 태그로 좁히기 / 밝기 전환 / `#e0425` 형태의 딥링크. 모두 키보드로 도달 가능하고 `aria-expanded`·`aria-pressed`로 상태를 알린다.

## 하지 않는 것

동일 규격 카드 그리드, 그라디언트 텍스트, 장식용 글래스, 1px 초과 색 좌측 보더, 섹션 번호, 제목 위 아이브로우, 장식용 스파크라인, 기술적으로 보이려는 모노스페이스.
