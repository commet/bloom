# Design

빌드된 `dist/index.html`에서 기록한 것. 의도가 아니라 실제로 화면에 있는 것을 적는다.

## 원칙

**평가하지 않는다.** 이 페이지는 참석자 한 사람이 공개 기록을 모은 것이고, 커뮤니티를 진단하거나
의미를 부여하는 문장은 한 줄도 넣지 않는다. 화면에 있는 모든 문장은 원본 기록에 있던 것이거나,
데이터에서 계산된 수치다.

**시각화가 먼저다.** 26회를 한 눈에 보여주는 것이 첫 화면의 일이고, 긴 내용은 드로어로 내려 스크롤을
줄인다. 본문 스크롤은 네 화면 안쪽이다.

## 세계

종이 흰색 위의 연감(almanac) 데이터 시트. 크롬(내비게이션·라벨·구분선)은 철저히 무채색이고,
색은 데이터에만 쓴다. 라이트가 기본이고 다크는 따로 고른 변형이다.

## 색

| 토큰 | 라이트 | 다크 | 역할 |
|---|---|---|---|
| `--surface-1` | `#fcfcfb` | `#1a1a19` | 바탕 |
| `--surface-2/3` | `#f3f3f0` / `#ffffff` | `#232322` / `#262624` | 돋운 면, 카드 |
| `--text-primary` | `#0b0b0b` | `#ffffff` | 본문 |
| `--text-secondary` | `#52514e` | `#c3c2b7` | 부가 |
| `--text-muted` | `#706e67` | `#93918a` | 라벨·주석 |
| `--border` / `--border-strong` | `#e4e3df` / `#c6c4be` | `#33332f` / `#4d4d47` | 괘선 |
| `--s1` 파랑 | `#2a78d6` | `#3987e5` | 라운드테이블 / 막대·선 |
| `--s2` 주황 | `#eb6834` | `#d95926` | 강연 중심 |
| `--s3` 청록 | `#1baf7a` | `#199e70` | 그 외 |
| `--ok` / `--warn` | `#14805a` / `#b34a12` | `#45b98a` / `#ef8b5e` | 상태 — 확인됨 / 미확인 |

`--s1/2/3`은 dataviz 스킬의 검증된 카테고리 팔레트에서 앞 세 슬롯을 그대로 쓴다. 세 슬롯은
`--pairs all` 기준으로 색각 이상 ΔE 9.2(라이트) / 9.4(다크), 정상 시야 ΔE 24.0 / 20.9를 통과한다.
`validate_palette.js`가 라이트에서 청록의 표면 대비를 3:1 미만으로 경고하므로, 범례와 직접 라벨을
반드시 함께 싣는다(색만으로 구분하지 않는다).

**데이터 색은 글자에 쓰지 않는다.** 상태 표시가 필요한 곳에는 `--ok` / `--warn`을 쓰고, 아이콘과
라벨을 함께 붙인다. 이 규칙을 어겼던 두 곳(예정 확인 줄, 미확인 항목)은 검사에서 잡혀 고쳤다.

## 활자

- **Archivo Variable** — 굵기 100–900, 너비 62–125% 두 축. 제목은 `wdth 112`, 통계는 `wdth 104`,
  대문자 라벨은 `wdth 88`. 모든 수치는 `tabular-nums`.
- **Gothic A1** 600 / 800 — 한글 제목과 인용문.
- 한글 본문은 시스템 스택(Apple SD Gothic Neo / Pretendard / Noto Sans KR). 읽기용 표면이므로
  워크호스 서체가 맞다.

두 웹폰트는 페이지에 실제로 쓰인 글자만 남겨 base64로 인라인한다(총 160KB woff2). 한글은
`word-break: keep-all`, 자간 하한은 디스플레이 −0.045em / 제목 −0.035em.

## 구성

1. **머리** — 제목, 한 줄 요약, 출처 고지, 네 개의 수치.
2. **달력** (`#cal`) — 가로 18주 × 세로 7요일. 칸 색은 형식 세 갈래, 예정은 외곽선, 오늘은 검은 테두리,
   행사 없는 날은 3px 점. 오른쪽에 세로축(요일)을 공유하는 **요일별 합계 막대**가 붙는다 —
   목 11회, 화 8회라는 실제 분포가 여기서 바로 읽힌다. 넓은 화면에서는 오른쪽에 **주요 이력** 목록이 붙어
   한 행을 채우고, 목록 높이를 달력 높이에 맞춰 잘라 내부에서만 스크롤한다(`syncHeroHeight`).
   두 칼럼 높이가 어긋나면 히어로가 한 화면을 넘어가고, 그다음 섹션이 한참 밀린다.
3. **발언 기록** — 상단에 **핵심어 인덱스**가 있다. 후보 단어 목록은 고정하되 등장 횟수는 기록
   전체(발언 35건 + 조별 토론 45건)에서 계산하며, 3회 이상 나온 상위 20개만 남긴다. 글자 크기가
   곧 빈도이고 위첨자가 실제 횟수다. 선택하면 그 단어가 등장한 기록만 남고 본문에서 해당 단어가
   `<mark>`로 표시된다. 처음 12장만 보이고 나머지는 버튼으로 펼친다.
4. **통계** — 월별 개최 횟수(형식별 누적) / 장소별 개최 횟수 / 공개된 규모 / 디스코드 가입자.
   참석과 신청은 **같은 단위(사람 수)**라 한 축을 공유하고 색과 범례로 계열을 구분한다.
   라벨이 길면 막대 위로 올라간다.
5. **다음**, **기록 기준과 검증**(접힘), 푸터.

## 드로어

행사 하나의 전체 기록은 오른쪽 드로어(모바일은 바텀 시트)에서 연다. 본문 스크롤이 늘어나지 않는
이유가 이것이다. `role="dialog"` + `aria-modal`, 열 때 닫기 버튼으로 포커스 이동, Tab 순환,
Esc·배경 클릭으로 닫고 원래 위치로 포커스 복귀. `#e0425` 형태의 딥링크를 지원한다.

## 움직임

드로어의 진입·퇴장(0.34s, `cubic-bezier(.16,1,.3,1)`) 하나만 연출한다. 카드는 hover에서 2px 뜨고,
달력 칸은 hover에서 살짝 옅어진다. 진입 애니메이션은 없다. `prefers-reduced-motion`에서 전부 정지.

## 참여

읽기만 하는 페이지에 두 개의 참여 지점을 뒀다. 둘 다 **기록 자체는 건드리지 않는다** — 방문자가
보낸 것과 원본 기록이 화면에서 섞이면 이 페이지의 전제가 무너진다.

1. **발언 공감** — 발언 카드마다 표시 하나. 익명이고, 브라우저가 만든 uuid 로 중복만 막는다.
   숫자는 카드 오른쪽 아래에 작게 붙고 0이면 표시하지 않는다. 공감순 정렬을 켜면 이 수치가
   순서를 정한다. 원본 기록이 아니라 읽는 사람의 반응이라는 게 보이도록, 색과 크기를 본문과
   분명히 다르게 뒀다.
2. **기록 보태기** — 정정 / 빠진 기록 / 함께 만들기 세 갈래. 보낸 내용은 검토 후 수동 반영하며
   페이지에 자동 노출되지 않는다는 것을 폼 옆에 적어뒀다.

**Supabase 접근 방식.** 테이블에 RLS 를 켜고 **정책을 하나도 두지 않아** anon 이 테이블을 직접
읽거나 쓸 수 없다. 대신 `SECURITY DEFINER` 함수 세 개(`bloom_mark_counts`, `bloom_mark`,
`bloom_note`)만 실행 권한을 준다. 함수 안에서 길이 검사, 브라우저당 공감 200개 상한,
제보 하루 5건 상한을 건다. 익명 키가 페이지에 박혀 있어도 이 세 동작 외에는 할 수 있는 게 없다.

**연결이 막힌 환경**(아티팩트 미리보기의 CSP, 사내망 등)에서는 공감 버튼이 아예 렌더되지 않고
폼은 비활성 상태로 안내 문구를 보여준다. 나머지 페이지는 그대로 동작한다.

## 「당신의 한 줄」 (`/line`)

회고 모임 하루짜리 페이지. 아카이브와 같은 토큰·서체·시간축을 쓰되 목적이 다르다 —
읽는 페이지가 아니라 **자기 말을 기록에 비춰 보는** 페이지다.

**입력은 하나다.** 오늘 조별 회고에서 가져갈 한 줄. 두 시간 이야기한 방에 다시 설문을
돌리지 않으려면 이미 입 밖으로 나온 것을 옮기는 정도여야 한다. 참석 이력에 기대지 않으므로
처음 온 사람도 똑같이 작동한다.

**출력은 카드다.** 그 줄이 놓인 갈래, 같은 갈래의 과거 문장 4~6줄(날짜·출처 포함),
그리고 순번. 어느 갈래에도 안 걸리면 "26회 기록에 없던 말" 이 되는데, 이쪽이 오히려
가져가고 싶은 결과라 실패로 다루지 않는다. PNG 로 저장된다.

**색은 두 개만 뜻을 가진다.** 4개월 기록은 `--border-strong`(무채색), 오늘 이 방은 `--s1`,
기록에 없던 말은 `--s2`. 세 가지 모두 발표자 화면에 직접 라벨과 범례를 함께 싣는다.
**글자에는 데이터 색을 쓰지 않는다** — 새 갈래 문구는 `--warn` 이다. 이 규칙을 어겼던 세 곳을
`tools/line-check.mjs` 의 대비 검사가 잡아냈다(아카이브에서 두 번 잡힌 것과 같은 실수).

**갈래 판정은 낱말 규칙이다.** 모델을 부르지 않는다. 점수는 걸린 낱말의 길이 합이라 긴 낱말이
더 세게 잡고, 하나도 안 걸리면 새 갈래다. 판정이 틀릴 수 있으므로 카드에 칩을 두어 본인이
고치게 했다 — 규칙이 조잡해도 화면이 깨지지 않는 이유가 이것이다. 판정 로직은 `src/lineage.js`
와 페이지 안에 같은 규칙으로 두 벌 있고, 검사가 두 벌의 결과가 어긋나지 않는지 본다.

**갈래 문장은 전부 원문이다.** `src/data.js` 안에 그 문자열이 그대로 있는지 검사가 확인한다.
갈래 구분 자체도 새로 나눈 것이 아니라 `data.js` 의 `themes` 가 이미 세워 둔 다섯 줄기다.

**오늘 나온 줄은 기록이 아니다.** 테이블도 페이지도 아카이브와 분리돼 있고, 화면에서도
"여기 적힌 줄은 오늘 이 방의 말" 이라고 밝힌다. 둘이 섞이면 아카이브의 전제가 무너진다.

**발표자 화면**(`?screen`)은 아카이브의 시간축을 그대로 쓴다(`GRID0`, 18주). 다섯 갈래 +
"기록에 없던 말" 여섯 트랙에 4개월치가 회색 점으로 깔려 있고, 제출될 때마다 오늘 자리에
점이 얹힌다. 트랙 높이는 남은 세로 공간에서 되돌려 풀어, 집계 줄이 잘리지 않는다.
QR 은 빌드 때 구워 SVG 로 심는다 — 런타임 인코더는 검증할 방법이 없다.

**운영진 몫은 덤으로 나온다.** 같은 입력에서 갈래별 분포와 "기록에 없던 말" 목록이 나오고,
버튼 하나로 클립보드에 담긴다. 이걸 위해 질문을 하나 더 던지는 순간 설문이 되므로 그러지 않는다.
한글은 폭이 제각각이라 자릿수를 맞추지 않는다 — 어디에 붙여 넣어도 어긋나지 않게.

## 이름 표기

실명은 **성만 노출**한다. 한글 이름은 성 + `○`(이○○), 라틴 이름은 성(姓)만(Cross, Min).
치환표는 **연사 명단과 인물 목록에 실제로 있는 이름**에서만 만든다 — 인용 출처에서 긁으면
‘운영진’·‘참석자’ 같은 역할어까지 이름으로 오인해 마스킹한다(실제로 한 번 그렇게 됐다).
활동명·필명(조코딩, 팔로알토, KEEPKWAN, 빌더 조쉬)은 그대로 둔다. 렌더 직전 `T()` 한 곳을
거치므로 데이터 원본은 손대지 않는다. `tools/check.mjs` 가 화면 텍스트에 전체 이름이 남아 있으면
실패시킨다.

## 한국어 조판

`word-break: keep-all` + `text-wrap: pretty` 로 어절이 쪼개지거나 마지막 한 단어만 떨어지는 것을
막는다. 머리말은 한 문장을 한 블록으로 두어 줄이 문장 중간에서 걸치지 않게 했다.

## 브라우저 표면

선택 영역, 캐럿, 스크롤바, 포커스 링, 밑줄 오프셋을 팔레트에서 지정한다. 아이콘은 그린 SVG만
쓴다 — 유니코드 글리프·이모지 없음.

## 검사

`tools/check.mjs`가 두 테마에서 대비(반투명 배경 합성 후 계산)와 상호작용을 확인하고,
`tools/audit.mjs`가 390 / 768 / 1440px에서 가로 넘침을 본다. 내부 스크롤 영역(달력)과 화면 밖
드로어는 의도된 것이므로 제외한다.
